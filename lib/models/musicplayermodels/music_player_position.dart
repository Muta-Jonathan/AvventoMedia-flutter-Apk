class MusicPlayerPosition{
  final Duration position;
  final Duration bufferedPosition;
  final Duration duration;

  const MusicPlayerPosition(this.position, this.bufferedPosition, this.duration);
}